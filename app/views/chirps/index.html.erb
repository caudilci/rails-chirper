<div>
  <h1 class="font-bold text-4xl">Chirps#index</h1>
  <p>Find me in app/views/chirps/index.html.erb</p>
  <%= form_for :chirp, url: "/chirps", method: :post do |form| %>
    <%= form.label :message, "Create a chirp:" %>
    <%= form.text_field :message %>
    <%= form.submit "Chirp" %>
  <% end %>
  <%= debug @chirps %>
</div>
